package com.data_access;

/**
 * Parses incoming raw data into a standard format.
 */
public class DataParser {

    /**
     * Parses incoming raw data.
     *
     * @param rawData the raw incoming data
     * @return the parsed data
     */
    public String parseData(String rawData) {

        return rawData.trim();
    }
}